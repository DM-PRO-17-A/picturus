#include <vector>
#include <algorithm> // for std::fill


int main()
{
	float[43] average;
	int i;
	for ( i = 0, i < 43; ++i )
		average[i] = 0.0;

	// Initialise camera program here? Or separately in Bash?

	while(1)
	{
		// Read from 
	}
	
	return 0;
}
